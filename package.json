{
  "name": "storage-value",
  "version": "0.0.8",
  "description": "Value using Storage.",
  "main": "lib/value.js",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "NODE_ENV=production babel src --out-dir lib --source-maps inline",
    "watch": "babel src --out-dir lib --watch --source-maps inline",
    "test": "mocha ./test",
    "prepublish": "npm run --if-present build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TheLus/storage-value.git"
  },
  "keywords": [
    "local-storage, session-storage"
  ],
  "author": "the_lus",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/TheLus/storage-value/issues"
  },
  "homepage": "https://github.com/TheLus/storage-value#readme",
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-plugin-transform-class-properties": "^6.10.2",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-power-assert": "^1.0.0",
    "babel-register": "^6.9.0",
    "lodash.debounce": "^4.0.6",
    "mocha": "^2.5.3",
    "mock-local-storage": "^1.0.2",
    "power-assert": "^1.4.1"
  }
}
